import styled from 'styled-components'
import Button from '@material-ui/core/Button'

export const HTMLInputFile = styled.input`
  display: none;
`

export const PreviewButton = styled(Button)`
  bottom: 0;
  left: 0;
  position: fixed;
  z-index: 2;
`
